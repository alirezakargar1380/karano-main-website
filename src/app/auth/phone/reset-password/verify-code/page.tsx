import { PhoneResetPasswordVerifyCodeView } from 'src/sections/auth/phone/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Phone: Reset Password Verify Phone',
};

export default function ResetPasswordVerifyCodePage() {
  return <PhoneResetPasswordVerifyCodeView />;
}
